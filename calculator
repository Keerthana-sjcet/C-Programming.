 /* Description  :A C program, which takes two integer operands and one operator form the user, performs the operation and then prints the result using Switch Statement)
 * Author       :   Keerthana G Pillai
 * Version      :   1.0
 * Date         :   26-01-2025
*/
#include <stdio.h>
int main() {
    int num1, num2,choice;
    printf("Enter two numbers: ");
    scanf("%d%d",&num1,&num2);
    printf("Enter any number of 1 for addition ,2 for subtraction,3 for multiplication,4 for division,5 for modulus");
    
    scanf("%d",&choice);
    switch (choice){
        case 1:
            printf("\n%d",num1 + num2);
            break;
        case 2:
            printf("%d\n", num1 - num2);
            break;
        case 3:
            printf(" %d\n", num1 * num2);
            break;
        case 4:
            printf("%d\n", num1 / num2);
            break;
        case 5:
            printf(" %d\n", num1 % num2);
            break;
        default:
            printf("Invalid");
    }

    return 0;
}
